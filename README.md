run backtest to see what settings to set today. recommended run few times a day and set settings.

2 bots same thing one is fixed threeshold bot_perp_nad30.py one is dynamic threeshold perp_bot_over_threeshold_untested.py.

Trade at your own risk!

start and see error paste to chatgpt usually to install packages 4 or 5 i think need to install with pip install package_name

first install python3 and python3-venv

then create virtual environment file python3 -m venv virt

then activate source virt/bin/activate

then start and paste error to chatgpt will tell you which packages to install 

and when all packages installed bot will start

if you like to receive update on bug fixes or bot update give it a star to this repo

tnx again
